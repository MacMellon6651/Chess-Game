заменить Logger на boost::Logger +

git подключить +

реализовать клииет (однопоточеный клиент) +-

разобраться с реккурсией

(Функция do_read() запускает операцию чтения и сразу завершается
Стек очищается
Когда приходят данные, Boost.Asio вызывает коллбэк (обработчик)
В коллбэке мы снова вызываем do_read() - но это происходит в новом стеке)



server.cpp shared_ptr  - шарянная память ?

(shared_ptr -  "учётчик" количества ссылок на объект
Каждый, кто пользуется объектом, увеличивает счётчи
Когда счётчик = 0 - объект автоматически удаляется
shared_from_this() создаёт такой "учётчик" из самого объекта)


gamemanager доделать реализацию до простого мессенджера (поправить отправку ходов!)

Protocol с switch интерфейсов чтобы можно было запускать и из под андроида и из браузера и т.д.

(Сделать нормальный подбор по условным дивизионам (ранкед система ) -> разработать систему начисления очков !)